import test from 'ava';
import * as helpStyle from './index.js';

// const element = new HTMLElement();
// const list = new NodeList();
// const className = 'btn-blue';

test('addClass()', (t) => {
	// helpStyle.addClass(element, className);
	t.pass();
});

test('addClassAll()', (t) => {
	// helpStyle.addClassAll(list, className);
	t.pass();
});

test('removeClass()', (t) => {
	// helpStyle.removeClass(element, className);
	t.pass();
});

test('removeClassAll()', (t) => {
	// helpStyle.removeClassAll(list, className);
	t.pass();
});

test('hasClass()', (t) => {
	// helpStyle.hasClass(element, className);
	t.pass();
});

test('toggleClass()', (t) => {
	// helpStyle.toggleClass(element, className);
	t.pass();
});

test('getStyle()', (t) => {
	// helpStyle.getStyle(element, className);
	t.pass();
});

test('getCurrentMediaQuery()', (t) => {
	// helpStyle.getCurrentMediaQuery();
	t.pass();
});

test('getMetaContentByName()', (t) => {
	// helpStyle.getMetaContentByName(className);
	t.pass();
});

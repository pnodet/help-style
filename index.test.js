import test from 'ava';

test('addClass()', (t) => {
	t.pass();
});

test('addClassAll()', (t) => {
	t.pass();
});

test('removeClass()', (t) => {
	t.pass();
});

test('removeClassAll()', (t) => {
	t.pass();
});

test('hasClass()', (t) => {
	t.pass();
});

test('toggleClass()', (t) => {
	t.pass();
});

test('getStyle()', (t) => {
	t.pass();
});

test('getCurrentMediaQuery()', (t) => {
	t.pass();
});

test('getMetaContentByName()', (t) => {
	t.pass();
});
